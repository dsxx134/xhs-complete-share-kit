遵循仓库根目录 `AGENTS.md` 作为最高优先级规则。

默认规则（适用于“关键词 + 高赞 + 最近7天 + 前N + 详细数据”）：
1) 每次运行前先执行：`bash scripts/kb_preflight.sh`
2) 确认 MCP 服务健康且已登录（`/health`、`/api/v1/login/status`）
3) 搜索近7天内容：优先 `sort_by=点赞最多`；若失败则回退 `综合` 并在本地按点赞重排
4) 拉取前N条详情，失败重试并保留错误字段
5) 输出富详情结果到 `output/`（JSON + MD）
6) 需要入库时按飞书表字段类型适配（URL 字段按对象格式）
7) 每次运行结束后执行：`python3 scripts/kb_append_log.py ...` 追加日志

语言要求：
- 默认所有说明、总结、提交信息、报告内容使用中文。
- 若用户明确要求英文，再切换为英文输出。
